import { Controller } from '@nestjs/common';

@Controller('user')
export class UserController {
  /*@Get()
  findAll(@Query() query: UserQuery): Promise<UserDto[]> {
    return this.userService.findAll(query);
  }*/
}
