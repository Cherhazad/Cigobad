{
  "name": "club-badminton",
  "private": true,
  "workspaces": [
    "front",
    "back",
    "shared"
  ],
  "scripts": {
    "dev:front": "npm run dev --workspace=front",
    "dev:back": "npm run dev --workspace=back",
    "build:shared": "npm run build --workspace=shared",
    "build": "npm run build --workspace=shared && npm run build --workspace=back && npm run build --workspace=front",
    "dev": "concurrently \"npm run dev:shared\" \"npm run dev:back\" \"npm run dev:front\""
  },
  "devDependencies": {
    "concurrently": "^10.0.4",
    "tsup": "^8.5.1",
    "typescript": "^6.0.3"
  }
}
